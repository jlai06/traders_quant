# Secrets - never commit these
.env
*.key

# Python
__pycache__/
*.pyc
.venv/
venv/

# Raw pulled market data - do not publicly redistribute licensed data.
# Keep the pipeline code, not the data itself, in version control.
data/raw/
*.csv
!decision_log.csv
!experiment_record.csv

# OS / editor noise
.DS_Store
.vscode/
